<li>
  <%= link_to version.number, rubygem_version_url(version.rubygem, version.slug) %>
  <small><%= l(version.built_at.to_date, :format => :long) %></small>
  <% if version.platformed? %>
    <span class="platform"><%= version.platform %></span>
  <% end %>
</li>
